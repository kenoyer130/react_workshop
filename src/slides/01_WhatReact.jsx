import Slide from '../Slide';

const slide =  Slide (`
# What is React?

* "React is a declarative, efficient, and flexible JavaScript library for building user interfaces." - facebook
* Javascript library put out by Facebook
* Presentational layer (the **View** in MVC)
* One-way data flow
* Virtual DOM 
`);     

export default slide;