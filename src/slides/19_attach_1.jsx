import Slide from '../Slide';

const slide =  Slide (`
# Attaching your root component to the dom
* React is two seperate libraries. React and ReactDOM
* This seperates the React engine from DOM output
* Used by Unit tests to output to JSDOM instead
* Used by React Native to output to Native code

`);     

export default slide;