import Slide from '../Slide';

const Body  =  Slide (`
# I tricked you all!
* Functional Stateless components
* Functions that return other functions
* Pure functions that return the same results with the same input
* Immutability
* Isolation and special handling of state
`);     

export default Body;