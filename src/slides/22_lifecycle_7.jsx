import Slide from '../Slide';

const Wrapper =  Slide (`
# The Component Lifecycle
* componentWillUpdate(nextProps, nextState)
    * Called when new props or state are passed in
    * Not called on initial render
`);     
  

export default Wrapper;