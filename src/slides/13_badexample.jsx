import Slide from '../Slide';

const slide =  Slide (`
# Disclaimer. Do not use these slide examples for production code!
## Almost to React!

* Only one Component per file
* JSDoc comments on all non trivial public functions
* Unit Tests on ALL Components
* Define PropTypes and DefaultProps for all Components (explained later)

`);     

export default slide;